<Window x:Class="ElloNote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:cp="clr-namespace:ColorPickerLib.Behaviours;assembly=ColorPickerLib"
        xmlns:cpwpf="clr-namespace:WPF.ColorPicker;assembly=WPF.ColorPicker"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm ="clr-namespace:ElloNote.ViewModels"
        xmlns:local="clr-namespace:ElloNote"
        mc:Ignorable="d"
        Title="ElloNote" Height="760" Width="1440" WindowStyle="None" WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        >
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    
    
    <Border CornerRadius="10"
            BorderThickness="1">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource winBorderColor1}" Offset="0"/>
                <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.543"/>
                <GradientStop Color="{StaticResource winBorderColor3}" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        
        <Border.Background>
            <LinearGradientBrush>
                <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="0"/>
                <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="0.543"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border CornerRadius="10,0,0,10" Grid.Column="0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="0"/>
                        <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                
                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <StackPanel Orientation="Horizontal"
                            Height="35"
                            Margin="15,25,0,40"
                            >
                        <TextBlock Text="AHX" Foreground="{StaticResource titleColor2}" FontSize="20" FontFamily="VDS"
                               FontWeight="Medium" VerticalAlignment="Center" Margin="50,0,0,0"/>

                    </StackPanel>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color1}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="Home"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Main" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color2}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="File"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Insert" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color6}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="Pencil"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Draw" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color9}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="Eye"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="View" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color2}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="Search"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Search" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color3}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="Clock"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Recent" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                    <RadioButton Style="{StaticResource menuButton}" Tag="{StaticResource color7}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <fa:IconImage Icon="Tools"  Style="{StaticResource menuButtonIcon}"/>
                            <TextBlock Text="Settings" Style="{StaticResource menuButtonText}"/>
                        </StackPanel>
                    </RadioButton>
                    
                </StackPanel>
            </Border>
            
            <Border Grid.Column="1" CornerRadius="0,10,10,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource primaryBackColor2}" Offset="0"/>
                        <GradientStop Color="{StaticResource primaryBackColor1}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel x:Name="pnlControlBar" Grid.Row="0" Orientation="Horizontal" FlowDirection="RightToLeft"
                        Background="Transparent" Margin="0,0,3,0"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                        <Button x:Name="closeButton" Style="{StaticResource controlBarButton}" 
                                Tag="{StaticResource color4}" Command="{Binding CloseApplicationCommand}" Width="40">
                            <fa:IconImage Icon="Close" Style="{StaticResource controlBarButtonIcon}"/>
                        </Button>

                        <Button x:Name="minimizeButton" Style="{StaticResource controlBarButton}" 
                                Tag="{StaticResource color4}" Command="{Binding MinimizedApplicationCommand}">
                            <fa:IconImage Icon="WindowMinimize" Style="{StaticResource controlBarButtonIcon}"/>
                        </Button>
                    </StackPanel>

                    <DockPanel x:Name="pnlDashboard" Grid.Row="1"
                                HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <WrapPanel>
                            <ToolBar Style="{DynamicResource dashboardToolBarStyle}"  Background="Transparent" ToolBarTray.IsLocked="True">

                                <Button x:Name="backButton" Style="{StaticResource dashboardBarButton}"
                                        Command="ApplicationCommands.Undo" ToolTip="Undo">
                                    <fa:IconImage Icon="RotateLeft" Style="{StaticResource dashboardBarButtonIcon}"/>
                                </Button>

                                <Button x:Name="nextButton" Style="{StaticResource dashboardBarButton}"
                                    Command="ApplicationCommands.Redo" ToolTip="Redo">
                                    <fa:IconImage Icon="RotateRight" Style="{StaticResource dashboardBarButtonIcon}"/>
                                </Button>
                            </ToolBar>
                            <Polygon Fill="{StaticResource plainTextColor3}" StrokeThickness="1" Stroke="{StaticResource plainTextColor3}"
                                     Points="0,0,0,35" Margin="3,0,0,0"/>
                            <Button x:Name="saveButton" Style="{StaticResource dashboardBarButton}" Command="{Binding SaveFileCommand}">
                                <fa:IconImage Icon="FileEdit" Style="{StaticResource dashboardBarButtonIcon}"/>
                            </Button>

                            <Button x:Name="openButton" Style="{StaticResource dashboardBarButton}" Command="{Binding OpenFileCommand}">
                                <fa:IconImage Icon="Folder" Style="{StaticResource dashboardBarButtonIcon}"/>
                            </Button>

                            <Polygon Fill="{StaticResource plainTextColor3}" StrokeThickness="1" Stroke="{StaticResource plainTextColor3}"
                                     Points="0,0,0,35" Margin="3,0,0,0"/>

                            <ComboBox x:Name="fontsComboBox" Style="{StaticResource ComboBoxFlatStyle}" 
                                      ItemsSource="{Binding Source={x:Static Fonts.SystemFontFamilies}}" SelectedIndex="0"/>

                            <ComboBox x:Name="valuesComboBox" Style="{StaticResource ComboBoxFlatStyle}" 
                                      ItemsSource="{Binding SourceList}" Margin="1,0,0,0" SelectedIndex="4" 
                                      SelectedItem="{Binding SelectedFontSize}"/>
                            <ToolBar Style="{DynamicResource dashboardToolBarStyle}"  Background="Transparent" ToolBarTray.IsLocked="True">
                                <ToggleButton x:Name="boldTextButton"
                                         Content="B" FontWeight="ExtraBold"
                                         Style="{StaticResource toggleButton}"
                                         Command="EditingCommands.ToggleBold"
                                         CommandTarget="{Binding ElementName=MainTextBox}"
                                         
                                         />

                                <ToggleButton x:Name="obliqueTextButton"
                                          Command="EditingCommands.ToggleItalic" 
                                          CommandTarget="{Binding ElementName=MainTextBox}"
                                         
                                         Style="{StaticResource toggleButton}" FontStyle="Italic" Content="K">
                                </ToggleButton>

                                <ToggleButton x:Name="underlinedTextButton" 
                                         FontWeight="Regular" 
                                         Style="{StaticResource toggleButton}"
                                         Command="EditingCommands.ToggleUnderline" 
                                         CommandTarget="{Binding ElementName=MainTextBox}">
                                    <TextBlock Text="U" TextDecorations="Underline"/>
                                </ToggleButton>
                            </ToolBar>

                            <xctk:ColorPicker Width="20" SelectedColor="{Binding SelectedColor}"  ShowDropDownButton="False"
                                              DisplayColorTooltip="True" Name="ColorPicker"
                                              Background="Transparent" DropDownBackground="{StaticResource panelColor}"
                                              BorderThickness="0" Margin="5,0,0,0"/>

                            <Polygon Fill="{StaticResource plainTextColor3}" StrokeThickness="1" Stroke="{StaticResource plainTextColor3}"
                                     Points="0,0,0,35" Margin="3,0,0,0"/>


                        </WrapPanel>
                    </DockPanel>
                    <Border Grid.Column="1" Grid.Row="2" Margin="0,1,0,0">
                        <DockPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

                            <xctk:RichTextBox x:Name="MainTextBox" Background="Transparent" BorderBrush="Transparent" 
                                         Width="Auto" Height="Auto" BorderThickness="0"
                                         Foreground="Black" 
                                         FontSize="{Binding SelectedFontSize}"
                                         FontFamily="{Binding Path=SelectedItem, ElementName=fontsComboBox}"
                                         Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         >

                                <xctk:RichTextBox.CommandBindings>
                                    <CommandBinding Command="EditingCommands.ToggleBold"/>
                                    <CommandBinding Command="EditingCommands.ToggleItalic"/>
                                    <CommandBinding Command="EditingCommands.ToggleUnderline" />
                                </xctk:RichTextBox.CommandBindings>
                                <xctk:RichTextBox.TextFormatter>
                                    <xctk:PlainTextFormatter/>
                                </xctk:RichTextBox.TextFormatter>
                            </xctk:RichTextBox>
                            
                        </DockPanel>

                    </Border>

                </Grid>
            </Border>

        </Grid>

    </Border>
</Window>
